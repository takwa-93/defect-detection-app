export const environment = {
  production: true,
  apiUrl: '/api', // Can use relative paths assuming single-domain deployment, or full domain
  socketUrl: ''    // Relative path for socket.io if deployed on same host
};
